jobs:
 build:
   docker:
     - image: maven:3.3-jdk-8
     
   working_directory: ~/repo
   
     
   steps:
     - checkout:
     - run:
        command: |
         pwd
         ls -a
     - run:
        command: mvn install

     - run:
        name: Run test via maven
        command: mvn test
